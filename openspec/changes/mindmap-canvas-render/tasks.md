## 1. Routing

- [x] 1.1 Add an editor route page (`/mindmaps/[mindmapId]`) that renders a client canvas component

## 2. Canvas Rendering

- [x] 2.1 Implement a `MindmapState` → React Flow nodes/edges adapter with a simple deterministic layout
- [x] 2.2 Render the mindmap in `@xyflow/react` with pan/zoom enabled
- [x] 2.3 Support selecting a node and store `selectedNodeId` in UI state

## 3. Verification

- [x] 3.1 Verify `pnpm -s test` and `pnpm -s build` pass

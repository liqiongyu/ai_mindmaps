## 1. UI

- [x] 1.1 Keep summary, add expandable ops details
- [x] 1.2 Add copy-ops button
- [x] 1.3 Display provider/model for assistant messages

## 2. Rollback

- [x] 2.1 Add history helper to travel to a history marker
- [x] 2.2 Capture rollback marker when applying AI ops
- [x] 2.3 Wire rollback button per assistant message

## 3. API (optional but recommended)

- [x] 3.1 Include provider/model in POST `/api/ai/chat` response

## 4. Verification

- [x] 4.1 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, `pnpm -s test`

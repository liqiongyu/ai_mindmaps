## 1. Sidebar Scope Toggle

- [x] 1.1 Add scope toggle (Global / Node) to `MindmapChatSidebar`
- [x] 1.2 Pass selected node context from editor to sidebar

## 2. Node-scoped Requests

- [x] 2.1 Send `/api/ai/chat` requests with `scope="node"` and `selectedNodeId`
- [x] 2.2 Disable node chat when no node is selected

## 3. Verification

- [x] 3.1 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, `pnpm -s test`

## 1. Left/Right Layout

- [x] 1.1 Implement left/right layout in `mindmapStateToFlow()` with deterministic side assignment
- [x] 1.2 Ensure collapsed filtering continues to work with the new layout

## 2. Tests & Verification

- [x] 2.1 Add unit coverage for left/right positioning invariants
- [x] 2.2 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, `pnpm -s test`

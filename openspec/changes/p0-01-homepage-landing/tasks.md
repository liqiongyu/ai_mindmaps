## 1. Home Page UI

- [x] 1.1 Replace `/` scaffold with product landing layout (nav + hero + CTA)
- [x] 1.2 Add quick-start section, FAQ (collapsible), and privacy/footer copy

## 2. Routing + Metadata

- [x] 2.1 Add a minimal `/try` route so the primary CTA never 404s
- [x] 2.2 Update document metadata (title/description) to match landing positioning

## 3. Verification

- [x] 3.1 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, and `pnpm -s test` pass

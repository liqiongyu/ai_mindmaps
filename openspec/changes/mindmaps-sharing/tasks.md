## 1. Public Read-only Page

- [x] 1.1 Add `/public/:slug` page that loads and renders a public mindmap
- [x] 1.2 Add a client viewer wrapper around `MindmapCanvas` for public view

## 2. Share UI

- [x] 2.1 Add “Share” action to the persisted editor
- [x] 2.2 Display/copy the generated share URL and support refresh

## 3. List UI

- [x] 3.1 Show share links for public mindmaps on `/mindmaps`

## 4. Verification

- [x] 4.1 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, `pnpm -s test`

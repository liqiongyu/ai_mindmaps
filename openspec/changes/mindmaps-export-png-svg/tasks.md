## 1. Setup

- [x] 1.1 Add a DOM-to-image dependency for PNG/SVG export
- [x] 1.2 Add small export helper utilities (filename/options)

## 2. Editor Export UI

- [x] 2.1 Expose the React Flow viewport element for export
- [x] 2.2 Add “Export PNG” and “Export SVG” actions with loading/error states

## 3. Verification

- [x] 3.1 Add unit tests for export helpers (where feasible)
- [x] 3.2 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, `pnpm -s test`

## 1. Editor Actions

- [x] 1.1 Add “Add sibling” action for selected non-root node
- [x] 1.2 Add “Move up / Move down” actions to reorder selected node among siblings

## 2. Tests & Verification

- [x] 2.1 Add unit coverage for `reorder_children` in `applyOperations()`
- [x] 2.2 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, `pnpm -s test`

## 1. Collapse / Expand

- [x] 1.1 Track collapsed node ids in the editor UI state
- [x] 1.2 Filter rendered flow graph by collapsed nodes (hide descendants)
- [x] 1.3 Add toolbar action to toggle collapse/expand for selected node

## 2. Tests & Verification

- [x] 2.1 Add unit test for collapsed filtering in `mindmapStateToFlow()`
- [x] 2.2 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, `pnpm -s test`

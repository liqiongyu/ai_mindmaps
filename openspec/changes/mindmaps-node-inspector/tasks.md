## 1. Node Inspector UI

- [ ] 1.1 Add a node inspector modal to the editor for editing title + notes of the selected node
- [ ] 1.2 Apply changes via ops (`rename_node`, `update_notes`) and ensure undo/redo works

## 2. Tests & Verification

- [ ] 2.1 Add unit coverage for any new helper logic (if introduced)
- [ ] 2.2 Verify `pnpm -s format:check`, `pnpm -s lint`, `pnpm -s typecheck`, `pnpm -s test`
